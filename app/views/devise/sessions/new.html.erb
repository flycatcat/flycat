<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<div class="flex items-start justify-center mt-20 bg-gray-200">
    <div class="px-8 py-6 mt-0 text-left bg-white shadow-lg">
        <div class="flex justify-center">
        </div>
        <h2 class="text-2xl font-bold text-center"><%= '歡迎來到Flycat出勤管理系統' %></h2>
         <h3 class="text-2xl font-bold text-center"><%= '請輸入以下資訊登入' %></h3>
        <form action="">
            <div class="mt-4">
                <div>
                    <label class="block" for="email"><%= t('電子郵件') %><label>
                        <%= f.email_field :email, autofocus: true, autocomplete: "off" ,class: "w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600"%>
                           
                </div>
                <div class="mt-4">
                    <label class="block"><%= t('密碼') %><label>
                        <%= f.password_field :password, autocomplete: "off" ,class: "w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600"%>
                </div>
                 <% if devise_mapping.rememberable? %>
                     <div class="field">
                     <%= f.check_box :remember_me %>
                     <%= f.label :remember_me %>
                     </div>
                     <% end %>
                    <%= render "devise/shared/links" %>
                    <div class="flex items-baseline justify-between">
                    <%= f.submit t("登入"), class: "block w-full px-4 py-2 mt-4 text-sm font-medium leading-5 text-center text-white transition-colors duration-150 bg-blue-600 border border-transparent rounded-lg active:bg-blue-600 hover:bg-blue-700 focus:outline-none focus:shadow-outline-blue" %>


                </div>

 
            </div>
        </form>
    </div>
</div>

<% end %>